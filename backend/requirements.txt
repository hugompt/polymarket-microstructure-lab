# polymarket-microstructure-lab backend — runtime dependencies (pinned).
# Python 3.12. Install: python -m venv .venv && .venv/bin/pip install -r requirements.txt
# (SQLite is built into Python; psycopg is only needed for the optional Postgres/Docker path.)
fastapi==0.138.0
uvicorn==0.49.0
starlette==1.3.1
sqlalchemy==2.0.51
alembic==1.18.4
httpx==0.28.1
websockets==16.0
pydantic==2.13.4
pydantic-settings==2.14.2
python-dotenv==1.2.2
structlog==26.1.0
anyio==4.14.0
psycopg[binary]==3.3.4
