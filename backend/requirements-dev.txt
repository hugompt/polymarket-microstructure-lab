# Development / test dependencies (pinned). Install on top of requirements.txt:
#   .venv/bin/pip install -r requirements.txt -r requirements-dev.txt
-r requirements.txt
pytest==9.1.1
pytest-asyncio==1.4.0
